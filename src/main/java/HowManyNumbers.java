import java.util.List;

public class HowManyNumbers {
    int[] digits = {1, 2, 3, 4, 5, 6, 7, 8, 9};
    public static List<Long> findAll(final int sumDigits, final int numDigits) {

        return null;
    }

/*    long findNumber(long leftAmount, int leftSum) {
        if (leftAmount == 1) return leftSum;
        if (leftAmount > 1) {
            long pick = digits[1];
            return pick * 10 + findNumber(, leftSum - pick);
        }
    }*/
}
